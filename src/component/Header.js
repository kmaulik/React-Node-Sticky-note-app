// src/components/common/Header.js

import React from 'react';  
import { Link} from 'react-router-dom';

const Header = () => {  
  return (
    <nav>
      {/* <Link to="/logout" className="btn btn-warning fa fa-logout">Logout</Link> */}
    </nav>
  );
};

export default Header;